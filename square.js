/**
 * Squares a number.
 * 
 * @since 5.0.0
 * @category Math
 * @param {number} base The base number to square.
 * @returns {number} Returns the squared value
 * @example
 * 
 * square(4)
 * // => 16
 */

function square(number) {
  return number * number
}

export default square
