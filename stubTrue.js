/**
 * This method returns `true`.
 *
 * @static
 * @since 4.13.0
 * @category Util
 * @returns {boolean} Returns `true`.
 * @example
 *
 * times(2, stubTrue);
 * // => [true, true]
 */
function stubTrue() {
  return true;
}

export default stubTrue;
