/**
 * This method returns `false`.
 *
 * @since 4.13.0
 * @category Util
 * @returns {boolean} Returns `false`.
 * @example
 *
 * times(2, stubFalse);
 * // => [false, false]
 */
function stubFalse() {
  return false;
}

export default stubFalse;
